apiVersion: v1
kind: Service
metadata:
  name: frontend-colaborador-service
  namespace: peopleportal
spec:
  selector:
    app: frontend-colaborador
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
